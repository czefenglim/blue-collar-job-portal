@startuml UC_EditProfile
skinparam conditionStyle diamond
title Activity: Edit Profile
|Job Seeker|
start
:Click Edit Profile;
if (Edit Section?) then (Personal Info)
  :Update Personal Info;
else
  if (Edit Section?) then (Address)
    :Update Address;
  else
    if (Edit Section?) then (Job Preferences)
      :Update Job Preferences;
    else
      if (Edit Section?) then (Resume)
        :Upload / Update Resume;
      else (Skills)
        :Update Skills;
      endif
    endif
  endif
endif
:Click Save Changes;
|System|
:Validate Data;
if (Valid?) then (yes)
  :Update Database;
  :Show Success Message;
else (no)
  :Show Validation Errors;
endif
stop
@enduml

